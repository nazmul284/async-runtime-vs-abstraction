# Exactly what this benchmark needs, pinned to what was measured.
# An earlier version of this file was a shared list carrying ruff, black, flake8,
# isort, mypy, ty, structlog and loguru - none of which this benchmark imports.
uvloop==0.22.1
trio==0.34.0
anyio==4.15.1
